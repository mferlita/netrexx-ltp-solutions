/* Chapter 7 */

-- Some arrays
initArray = Rexx[0]
anotherArray = [5]
andAnotherArray = ["Hello", "Goodbye"]

flavor = "vanilla" -- Not an array, of course...
flavorArray = [89.9, flavor, 'flavor'] -- ...but this is

-- It is important to note that Ruby is dynamically typed and NetRexx is
-- statically typed. In ruby, you can mix types (booleans, other arrays, 
-- strings, etc) in an array because everything is an object. In NetRexx, 
-- everything in the array must be the same type. 

gems = ["Pearl", "Garnet", "Amethyst"]

SAY gems
SAY
SAY gems[0]
SAY gems[1]
SAY gems[2]
SAY gems[3] --This is out of range and returns NIL
SAY

other_gems = Rexx[3]
other_gems[2] = "Ruby"
other_gems[0] = "Pink Diamond"
other_gems[1] = "Sapphire"
other_gems[0] = "Rose Quartz"
SAY other_gems
SAY

-- My First Iterator
languages = ["English", "Norwegian", "Ruby"]

LOOP lang OVER languages
    SAY "I love" lang || "! Don't you?"
END

SAY "And let's hear it for Fortran!"
SAY "<crickets chirp in the distance>"
SAY

-- integer method FTW
LOOP i = 1 TO 3
  SAY "Hip-Hip-Hooray!"
END

SAY

LOOP i = 1 TO 2
  SAY "...you can SAY that again..."
END
SAY

-- More Array Methods
foods = ["artichoke", "brioche", "caramel"]
test = Date()
SAY foods
SAY
/* SAY foods.string -- this is like telling ooRexx "tell me what this object is." It will print out "an Array"
SAY test.string
SAY
SAY foods.makeString("L", ", ")
SAY
SAY foods.makeString("L", " :) ") || " 8)"
SAY

-- ooRexx will print 200 new lines if you do this.
--LOOP 200
--  SAY .Array~of()
--END
--
--SAY

-- ooRexx will only tell you what these objects in this array are.
gems_langs_foods = [gems, languages, foods]

SAY gems_langs_foods
SAY

-- In order to print out what's in these nested arrays, we have to write our own LOOP.
LOOP group OVER gems_langs_foods
    SAY group.makeString("L")
END
SAY

-- push and pop in Ruby become append and remove(array_name~last) in ooRexx.
-- last becomes lastItem whereas length becomes items.
favorites = Rexx[2]
favorites.append("raindrops on roses")
favorites.append("whiskey on kittens")

SAY favorites[1]
SAY favorites.lastItem
SAY favorites.items

SAY favorites.remove(favorites.last)
SAY favorites
SAY favorites.items */

/* Ch. 7 continued in ch_7_bld_and_sort_array.nrx, ch_7_table_of_contents_redux.nrx,
   and ch_7_make_a_list_no_sort.nrx */
